# @npp/domain-types

Owned by the shared platform foundation.

This package carries the monorepo's minimal core domain constants and shared enums for HTTP and role ownership.
It intentionally does not define business modules or inventory/sales logic.
