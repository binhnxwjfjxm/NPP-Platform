# @npp/test-helpers

Owned by the shared platform foundation.

This package provides tiny mock request and pool constructors for Core API and package-level regression tests.
It keeps unit tests independent from any real database or runtime host.
