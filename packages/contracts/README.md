# @npp/contracts

Owned by the monorepo shared platform foundation.

This package provides minimal API envelope helpers used by NPP Core services and downstream workspace consumers.
It intentionally keeps the type surface tiny and business-agnostic.
