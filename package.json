{
  "name": "npp-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "workspaces": [
    "mcp/apps/backend"
  ],
  "scripts": {
    "build": "npm --workspace mcp-plan-backend run build",
    "start": "npm --workspace mcp-plan-backend run start",
    "heroku-postbuild": "npm run build"
  }
}
