name: Provider Retirement Staging

on:
  pull_request:
    paths-ignore:
      - ".github/provider-retirement/**"
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  staging-only:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR superseded by clean Heroku PostgreSQL cutover branch."
