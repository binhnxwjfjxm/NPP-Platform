name: Generate MCP PostgreSQL parity migration

on:
  pull_request:
    branches: [main]
    paths:
      - mcp/ops/generate-postgresql-supabase-parity.mjs
      - mcp/supabase/migrations/**
      - .github/workflows/generate-mcp-postgresql-parity.yml

permissions:
  contents: write

jobs:
  generate:
    if: github.event.pull_request.head.ref == 'agent/mcp-supabase-write-parity'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate canonical and packaged migration
        run: node mcp/ops/generate-postgresql-supabase-parity.mjs

      - name: Commit generated migration
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- database/migrations/mcp/003_mcp_supabase_contract_parity.sql mcp/apps/backend/foundation/migrations/sql/003_mcp_supabase_contract_parity.sql; then
            echo 'Generated migration is current.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add database/migrations/mcp/003_mcp_supabase_contract_parity.sql mcp/apps/backend/foundation/migrations/sql/003_mcp_supabase_contract_parity.sql
          git commit -m 'feat(mcp): generate Supabase PostgreSQL parity migration'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
