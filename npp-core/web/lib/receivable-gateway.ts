import 'server-only';
import { randomUUID } from 'node:crypto';
import { requireNppWorkforceSessionToken } from './internal-auth-client';
const REQUEST_ID_PATTERN=/^[A-Za-z0-9._:-]{1,128}$/;const UUID_PATTERN=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const REQUEST_TIMEOUT_MS=8_000;const ALLOWED_QUERY_KEYS=new Set(['limit','offset','status','customerId','warehouseId','sourceDocumentType','search']);type CoreEnvelope<T>={data?:T;error?:{code?:string;message?:string;retryable?:boolean;details?:unknown};requestId?:string};export class ReceivableGatewayError extends Error{constructor(public readonly code:string,public readonly publicMessage:string,public readonly statusCode:number,public readonly retryable:boolean,public readonly details:unknown={}){super(publicMessage);this.name='ReceivableGatewayError';}}export function resolveReceivableRequestId(value:string|null|undefined){const c=String(value??'').trim();return REQUEST_ID_PATTERN.test(c)?c:`web_${randomUUID()}`;}function requiredServerValue(name:'CORE_API_INTERNAL_URL'|'CORE_API_SERVER_TOKEN'){if(name==='CORE_API_SERVER_TOKEN')return requireNppWorkforceSessionToken();const v=process.env[name]?.trim();if(!v)throw new ReceivableGatewayError('RECEIVABLE_GATEWAY_NOT_CONFIGURED','Công nợ khách hàng chưa được cấu hình',503,false);return v;}function coreApiBaseUrl(){let u:URL;try{u=new URL(requiredServerValue('CORE_API_INTERNAL_URL'));}catch{throw new ReceivableGatewayError('RECEIVABLE_GATEWAY_NOT_CONFIGURED','Công nợ khách hàng chưa được cấu hình',503,false);}if(!['http:','https:'].includes(u.protocol)||u.username||u.password||(process.env.NODE_ENV==='production'&&u.protocol!=='https:'))throw new ReceivableGatewayError('RECEIVABLE_GATEWAY_NOT_CONFIGURED','Công nợ khách hàng chưa được cấu hình',503,false);u.pathname=u.pathname.replace(/\/$/,'');u.search='';u.hash='';return u.toString().replace(/\/$/,'');}function assertUuid(value:string){const n=value.trim();if(!UUID_PATTERN.test(n))throw new ReceivableGatewayError('INVALID_RECEIVABLE_DOCUMENT_ID','Mã chứng từ công nợ không hợp lệ',400,false);return n;}function safeQuery(params:URLSearchParams){const q=new URLSearchParams();for(const[k,v]of params.entries())if(ALLOWED_QUERY_KEYS.has(k)&&v.length<=256)q.append(k,v);const s=q.toString();return s?`?${s}`:'';}async function requestCore<T>(path:string,requestId:string,params?:URLSearchParams){const c=new AbortController();const t=setTimeout(()=>c.abort(),REQUEST_TIMEOUT_MS);try{const r=await fetch(`${coreApiBaseUrl()}${path}${params?safeQuery(params):''}`,{method:'GET',cache:'no-store',signal:c.signal,headers:{Authorization:`Bearer ${requireNppWorkforceSessionToken()}`,Accept:'application/json','x-request-id':requestId}});let p:CoreEnvelope<T>;try{p=await r.json() as CoreEnvelope<T>;}catch{throw new ReceivableGatewayError('RECEIVABLE_GATEWAY_RESPONSE_INVALID','Phản hồi công nợ khách hàng không hợp lệ',502,false);}if(!r.ok)throw new ReceivableGatewayError(p.error?.code||'RECEIVABLE_REQUEST_FAILED',p.error?.message||'Yêu cầu công nợ khách hàng không thành công',r.status,p.error?.retryable===true,p.error?.details??{});if(!Object.prototype.hasOwnProperty.call(p,'data'))throw new ReceivableGatewayError('RECEIVABLE_GATEWAY_RESPONSE_INVALID','Phản hồi công nợ khách hàng không hợp lệ',502,false);return p.data as T;}catch(e){if(e instanceof ReceivableGatewayError)throw e;throw new ReceivableGatewayError('RECEIVABLE_GATEWAY_UNAVAILABLE','Công nợ khách hàng tạm thời chưa khả dụng',503,true);}finally{clearTimeout(t);}}function queryFrom(params?:Record<string,string|number|undefined>){const q=new URLSearchParams();for(const[k,v]of Object.entries(params??{}))if(v!==undefined&&v!=='')q.set(k,String(v));return q;}export function listReceivables<T>(requestId:string,params?:Record<string,string|number|undefined>):Promise<T[]>{return requestCore<T[]>('/api/receivables',requestId,queryFrom(params));}export function listReceivableBalances<T>(requestId:string,params?:Record<string,string|number|undefined>):Promise<T[]>{return requestCore<T[]>('/api/receivables/balances',requestId,queryFrom(params));}export function getReceivable<T>(id:string,requestId:string):Promise<T>{return requestCore<T>(`/api/receivables/${assertUuid(id)}`,requestId);}
