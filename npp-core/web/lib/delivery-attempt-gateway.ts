import 'server-only';
import { InventoryGatewayError } from './inventory-gateway';
import { requireNppWorkforceSessionToken } from './internal-auth-client';
const UUID_PATTERN=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const REQUEST_TIMEOUT_MS=30_000;interface CoreEnvelope<T>{data?:T;error?:{code?:string;message?:string;retryable?:boolean;details?:unknown}}
function baseUrl(){const raw=process.env.CORE_API_INTERNAL_URL?.trim();if(!raw)throw new InventoryGatewayError('DELIVERY_ATTEMPT_GATEWAY_NOT_CONFIGURED','Cổng kết quả giao chưa được cấu hình',503,false);let u:URL;try{u=new URL(raw);}catch{throw new InventoryGatewayError('DELIVERY_ATTEMPT_GATEWAY_NOT_CONFIGURED','Cổng kết quả giao chưa được cấu hình',503,false);}if(!['http:','https:'].includes(u.protocol)||u.username||u.password||(process.env.NODE_ENV==='production'&&u.protocol!=='https:'))throw new InventoryGatewayError('DELIVERY_ATTEMPT_GATEWAY_NOT_CONFIGURED','Cổng kết quả giao chưa được cấu hình',503,false);u.pathname=u.pathname.replace(/\/$/,'');u.search='';u.hash='';return u.toString().replace(/\/$/,'');}async function getCoreData<T>(path:string,requestId:string):Promise<T>{const c=new AbortController();const t=setTimeout(()=>c.abort(),REQUEST_TIMEOUT_MS);try{const r=await fetch(`${baseUrl()}${path}`,{method:'GET',cache:'no-store',signal:c.signal,headers:{Authorization:`Bearer ${requireNppWorkforceSessionToken()}`,Accept:'application/json','x-request-id':requestId}});let p:CoreEnvelope<T>;try{p=await r.json() as CoreEnvelope<T>;}catch{throw new InventoryGatewayError('DELIVERY_ATTEMPT_GATEWAY_RESPONSE_INVALID','Phản hồi kết quả giao không hợp lệ',502,false);}if(!r.ok)throw new InventoryGatewayError(p.error?.code||'DELIVERY_ATTEMPT_REQUEST_FAILED',p.error?.message||'Không tải được kết quả giao',r.status,p.error?.retryable===true,p.error?.details??{});if(!Object.prototype.hasOwnProperty.call(p,'data'))throw new InventoryGatewayError('DELIVERY_ATTEMPT_GATEWAY_RESPONSE_INVALID','Phản hồi kết quả giao không hợp lệ',502,false);return p.data as T;}catch(e){if(e instanceof InventoryGatewayError)throw e;throw new InventoryGatewayError('DELIVERY_ATTEMPT_GATEWAY_UNAVAILABLE','Cổng kết quả giao tạm thời không khả dụng',503,true);}finally{clearTimeout(t);}}export async function getDeliveryAttemptSummary<T>(tripId:string,requestId:string):Promise<T>{if(!UUID_PATTERN.test(tripId))throw new InventoryGatewayError('INVALID_TRIP_ID','Mã chuyến giao không hợp lệ',400,false);return getCoreData<T>(`/api/logistics/trips/${encodeURIComponent(tripId)}/attempts`,requestId);}export async function getDeliveryAttemptProofs<T>(tripId:string,attemptId:string,requestId:string):Promise<T>{if(!UUID_PATTERN.test(tripId)||!UUID_PATTERN.test(attemptId))throw new InventoryGatewayError('INVALID_POD_LINEAGE','Mã chuyến hoặc lần giao không hợp lệ',400,false);return getCoreData<T>(`/api/logistics/trips/${encodeURIComponent(tripId)}/attempts/${encodeURIComponent(attemptId)}/pod`,requestId);}
