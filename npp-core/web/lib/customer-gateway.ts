import 'server-only';
import { randomUUID } from 'node:crypto';
import { logGatewayFailure } from './gateway-diagnostics';
import { requireNppWorkforceSessionToken } from './internal-auth-client';
const REQUEST_ID_PATTERN=/^[A-Za-z0-9._:-]{1,128}$/;const UUID_PATTERN=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const REQUEST_TIMEOUT_MS=8_000;const ALLOWED_QUERY_KEYS=new Set(['active','limit','offset','search','groupId']);interface CoreEnvelope<T>{data?:T;error?:{code?:string;message?:string;retryable?:boolean;details?:unknown};requestId?:string}
export class CustomerGatewayError extends Error{constructor(public readonly code:string,public readonly publicMessage:string,public readonly statusCode:number,public readonly retryable:boolean,public readonly details:unknown={}){super(publicMessage);this.name='CustomerGatewayError';}}
export function resolveCustomerRequestId(value:string|null|undefined){const c=String(value??'').trim();return REQUEST_ID_PATTERN.test(c)?c:`web_${randomUUID()}`;}export function normalizeCustomerGatewayError(error:unknown){return error instanceof CustomerGatewayError?error:new CustomerGatewayError('CUSTOMER_GATEWAY_UNAVAILABLE','Dữ liệu khách hàng tạm thời chưa sẵn sàng',503,true);}
function requiredServerValue(name:'CORE_API_INTERNAL_URL'|'CORE_API_SERVER_TOKEN'){if(name==='CORE_API_SERVER_TOKEN')return requireNppWorkforceSessionToken();const v=process.env[name]?.trim();if(!v)throw new CustomerGatewayError('CUSTOMER_GATEWAY_NOT_CONFIGURED','Cổng khách hàng chưa được cấu hình',503,false);return v;}function coreApiBaseUrl(){let u:URL;try{u=new URL(requiredServerValue('CORE_API_INTERNAL_URL'));}catch{throw new CustomerGatewayError('CUSTOMER_GATEWAY_NOT_CONFIGURED','Cổng khách hàng chưa được cấu hình',503,false);}if(!['http:','https:'].includes(u.protocol)||u.username||u.password||(process.env.NODE_ENV==='production'&&u.protocol!=='https:'))throw new CustomerGatewayError('CUSTOMER_GATEWAY_NOT_CONFIGURED','Cổng khách hàng chưa được cấu hình',503,false);u.pathname=u.pathname.replace(/\/$/,'');u.search='';u.hash='';return u.toString().replace(/\/$/,'');}function safeQuery(p:URLSearchParams){const n=new URLSearchParams();for(const[k,v]of p.entries())if(ALLOWED_QUERY_KEYS.has(k)&&v.length<=256)n.append(k,v);const s=n.toString();return s?`?${s}`:'';}function assertUuid(value:string,code:string,message:string){const n=value.trim();if(!UUID_PATTERN.test(n))throw new CustomerGatewayError(code,message,400,false);return n;}function customerPath(id?:string){return `/api/customers${id?`/${assertUuid(id,'INVALID_CUSTOMER_ID','Mã khách hàng không hợp lệ')}`:''}`;}function groupPath(id?:string){return `/api/customer-groups${id?`/${assertUuid(id,'INVALID_CUSTOMER_GROUP_ID','Mã nhóm khách hàng không hợp lệ')}`:''}`;}function addressPath(customerId:string,addressId?:string){const c=assertUuid(customerId,'INVALID_CUSTOMER_ID','Mã khách hàng không hợp lệ');const a=addressId?`/${assertUuid(addressId,'INVALID_CUSTOMER_ADDRESS_ID','Mã địa chỉ không hợp lệ')}`:'';return `/api/customers/${c}/addresses${a}`;}
async function requestCore<T>({method,path,requestId,searchParams,body,idempotencyKey}:{method:'GET'|'POST'|'PATCH';path:string;requestId:string;searchParams?:URLSearchParams;body?:unknown;idempotencyKey?:string}){const c=new AbortController();const t=setTimeout(()=>c.abort(),REQUEST_TIMEOUT_MS);try{const r=await fetch(`${coreApiBaseUrl()}${path}${searchParams?safeQuery(searchParams):''}`,{method,cache:'no-store',signal:c.signal,headers:{Authorization:`Bearer ${requiredServerValue('CORE_API_SERVER_TOKEN')}`,Accept:'application/json','x-request-id':requestId,...(body===undefined?{}:{'Content-Type':'application/json'}),...(idempotencyKey?{'Idempotency-Key':idempotencyKey}:{})},...(body===undefined?{}:{body:JSON.stringify(body)})});let p:CoreEnvelope<T>;try{p=await r.json() as CoreEnvelope<T>;}catch{throw new CustomerGatewayError('CUSTOMER_GATEWAY_RESPONSE_INVALID','Phản hồi khách hàng không hợp lệ',502,false);}if(!r.ok)throw new CustomerGatewayError(p.error?.code||'CUSTOMER_REQUEST_FAILED',p.error?.message||'Yêu cầu khách hàng không thành công',r.status,p.error?.retryable===true,p.error?.details??{});if(!Object.prototype.hasOwnProperty.call(p,'data'))throw new CustomerGatewayError('CUSTOMER_GATEWAY_RESPONSE_INVALID','Phản hồi khách hàng không hợp lệ',502,false);return p.data as T;}catch(e){const n=e instanceof CustomerGatewayError?e:new CustomerGatewayError('CUSTOMER_GATEWAY_UNAVAILABLE','Cổng khách hàng tạm thời không khả dụng',503,true);logGatewayFailure({gateway:'customer',method,upstreamPath:path,status:n.statusCode,requestId,code:n.code});throw n;}finally{clearTimeout(t);}}
export function listAllCustomers<T>(requestId:string,searchParams=new URLSearchParams()):Promise<T[]>{return requestCore<T[]>({method:'GET',path:customerPath(),requestId,searchParams});}export function getCustomer<T>(id:string,requestId:string):Promise<T>{return requestCore<T>({method:'GET',path:customerPath(id),requestId});}export function createCustomer<T>(requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestCore<T>({method:'POST',path:customerPath(),requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}export function patchCustomer<T>(id:string,requestId:string,body:unknown):Promise<T>{return requestCore<T>({method:'PATCH',path:customerPath(id),requestId,body});}export function listCustomerGroups<T>(requestId:string,searchParams=new URLSearchParams()):Promise<T[]>{return requestCore<T[]>({method:'GET',path:groupPath(),requestId,searchParams});}export function getCustomerGroup<T>(id:string,requestId:string):Promise<T>{return requestCore<T>({method:'GET',path:groupPath(id),requestId});}export function createCustomerGroup<T>(requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestCore<T>({method:'POST',path:groupPath(),requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}export function patchCustomerGroup<T>(id:string,requestId:string,body:unknown):Promise<T>{return requestCore<T>({method:'PATCH',path:groupPath(id),requestId,body});}export function listCustomerAddresses<T>(customerId:string,requestId:string):Promise<T[]>{return requestCore<T[]>({method:'GET',path:addressPath(customerId),requestId});}export function createCustomerAddress<T>(customerId:string,requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestCore<T>({method:'POST',path:addressPath(customerId),requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}export function patchCustomerAddress<T>(customerId:string,addressId:string,requestId:string,body:unknown):Promise<T>{return requestCore<T>({method:'PATCH',path:addressPath(customerId,addressId),requestId,body});}
