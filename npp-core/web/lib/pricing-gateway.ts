import 'server-only';
import { randomUUID } from 'node:crypto';
import { requireNppWorkforceSessionToken } from './internal-auth-client';
const REQUEST_ID_PATTERN=/^[A-Za-z0-9._:-]{1,128}$/;const UUID_PATTERN=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const REQUEST_TIMEOUT_MS=8_000;const ALLOWED_QUERY_KEYS=new Set(['active','limit','offset','search','listType','currencyCode','variantId']);
interface CoreEnvelope<T>{data?:T;error?:{code?:string;message?:string;retryable?:boolean;details?:unknown};requestId?:string}
export class PricingGatewayError extends Error{constructor(public readonly code:string,public readonly publicMessage:string,public readonly statusCode:number,public readonly retryable:boolean,public readonly details:unknown={}){super(publicMessage);this.name='PricingGatewayError';}}
export function resolvePricingRequestId(value:string|null|undefined){const c=String(value??'').trim();return REQUEST_ID_PATTERN.test(c)?c:`web_${randomUUID()}`;}export function normalizePricingGatewayError(error:unknown){return error instanceof PricingGatewayError?error:new PricingGatewayError('PRICING_GATEWAY_UNAVAILABLE','Dữ liệu giá bán tạm thời chưa sẵn sàng',503,true);}
function requiredServerValue(name:'CORE_API_INTERNAL_URL'|'CORE_API_SERVER_TOKEN'){if(name==='CORE_API_SERVER_TOKEN')return requireNppWorkforceSessionToken();const v=process.env[name]?.trim();if(!v)throw new PricingGatewayError('PRICING_GATEWAY_NOT_CONFIGURED','Cổng giá bán chưa được cấu hình',503,false);return v;}function coreApiBaseUrl(){let u:URL;try{u=new URL(requiredServerValue('CORE_API_INTERNAL_URL'));}catch{throw new PricingGatewayError('PRICING_GATEWAY_NOT_CONFIGURED','Cổng giá bán chưa được cấu hình',503,false);}if(!['http:','https:'].includes(u.protocol)||u.username||u.password||(process.env.NODE_ENV==='production'&&u.protocol!=='https:'))throw new PricingGatewayError('PRICING_GATEWAY_NOT_CONFIGURED','Cổng giá bán chưa được cấu hình',503,false);u.pathname=u.pathname.replace(/\/$/,'');u.search='';u.hash='';return u.toString().replace(/\/$/,'');}function safeQuery(p:URLSearchParams){const n=new URLSearchParams();for(const[k,v]of p.entries())if(ALLOWED_QUERY_KEYS.has(k)&&v.length<=256)n.append(k,v);const s=n.toString();return s?`?${s}`:'';}function assertUuid(value:string,code:string,message:string){const n=String(value??'').trim();if(!UUID_PATTERN.test(n))throw new PricingGatewayError(code,message,400,false);return n;}
async function requestCore<T>({method,path,requestId,searchParams,body,idempotencyKey}:{method:'GET'|'POST'|'PATCH';path:string;requestId:string;searchParams?:URLSearchParams;body?:unknown;idempotencyKey?:string}){const c=new AbortController();const t=setTimeout(()=>c.abort(),REQUEST_TIMEOUT_MS);try{const r=await fetch(`${coreApiBaseUrl()}${path}${searchParams?safeQuery(searchParams):''}`,{method,cache:'no-store',signal:c.signal,headers:{Authorization:`Bearer ${requiredServerValue('CORE_API_SERVER_TOKEN')}`,Accept:'application/json','x-request-id':requestId,...(body===undefined?{}:{'Content-Type':'application/json'}),...(idempotencyKey?{'Idempotency-Key':idempotencyKey}:{})},...(body===undefined?{}:{body:JSON.stringify(body)})});let p:CoreEnvelope<T>;try{p=await r.json() as CoreEnvelope<T>;}catch{throw new PricingGatewayError('PRICING_GATEWAY_RESPONSE_INVALID','Phản hồi giá bán không hợp lệ',502,false);}if(!r.ok)throw new PricingGatewayError(p.error?.code||'PRICING_REQUEST_FAILED',p.error?.message||'Yêu cầu giá bán không thành công',r.status,p.error?.retryable===true,p.error?.details??{});if(!Object.prototype.hasOwnProperty.call(p,'data'))throw new PricingGatewayError('PRICING_GATEWAY_RESPONSE_INVALID','Phản hồi giá bán không hợp lệ',502,false);return p.data as T;}catch(e){if(e instanceof PricingGatewayError)throw e;throw new PricingGatewayError('PRICING_GATEWAY_UNAVAILABLE','Cổng giá bán tạm thời không khả dụng',503,true);}finally{clearTimeout(t);}}
export function listSalesChannels<T>(requestId:string,searchParams=new URLSearchParams()):Promise<T[]>{return requestCore<T[]>({method:'GET',path:'/api/sales-channels',requestId,searchParams});}export function createSalesChannel<T>(requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestCore<T>({method:'POST',path:'/api/sales-channels',requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}export function patchSalesChannel<T>(id:string,requestId:string,body:unknown):Promise<T>{return requestCore<T>({method:'PATCH',path:`/api/sales-channels/${assertUuid(id,'INVALID_CHANNEL_ID','Mã kênh bán không hợp lệ')}`,requestId,body});}export function listPriceLists<T>(requestId:string,searchParams=new URLSearchParams()):Promise<T[]>{return requestCore<T[]>({method:'GET',path:'/api/price-lists',requestId,searchParams});}export function createPriceList<T>(requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestCore<T>({method:'POST',path:'/api/price-lists',requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}export function patchPriceList<T>(id:string,requestId:string,body:unknown):Promise<T>{return requestCore<T>({method:'PATCH',path:`/api/price-lists/${assertUuid(id,'INVALID_PRICE_LIST_ID','Mã bảng giá không hợp lệ')}`,requestId,body});}export function listPriceListItems<T>(priceListId:string,requestId:string,searchParams=new URLSearchParams()):Promise<T[]>{return requestCore<T[]>({method:'GET',path:`/api/price-lists/${assertUuid(priceListId,'INVALID_PRICE_LIST_ID','Mã bảng giá không hợp lệ')}/items`,requestId,searchParams});}export function createPriceListItem<T>(priceListId:string,requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestCore<T>({method:'POST',path:`/api/price-lists/${assertUuid(priceListId,'INVALID_PRICE_LIST_ID','Mã bảng giá không hợp lệ')}/items`,requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}export function patchPriceListItem<T>(priceListId:string,itemId:string,requestId:string,body:unknown):Promise<T>{return requestCore<T>({method:'PATCH',path:`/api/price-lists/${assertUuid(priceListId,'INVALID_PRICE_LIST_ID','Mã bảng giá không hợp lệ')}/items/${assertUuid(itemId,'INVALID_PRICE_ITEM_ID','Mã quy tắc giá không hợp lệ')}`,requestId,body});}export function resolvePrice<T>(requestId:string,body:unknown):Promise<T>{return requestCore<T>({method:'POST',path:'/api/pricing/resolve',requestId,body});}export function importPricing<T>(requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestCore<T>({method:'POST',path:'/api/pricing/import',requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}
