import 'server-only';
import { randomUUID } from 'node:crypto';
import { InventoryGatewayError } from './inventory-gateway';
import { requireNppWorkforceSessionToken } from './internal-auth-client';
const UUID_PATTERN=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const REQUEST_TIMEOUT_MS=30_000;const ALLOWED_QUERY_KEYS=new Set(['status','salesOrderId','deliveryOrderId','limit','offset']);interface CoreEnvelope<T>{data?:T;error?:{code?:string;message?:string;retryable?:boolean;details?:unknown}}
function baseUrl(){const raw=process.env.CORE_API_INTERNAL_URL?.trim();if(!raw)throw new InventoryGatewayError('DELIVERY_ORDER_GATEWAY_NOT_CONFIGURED','Cổng giao nhận chưa được cấu hình',503,false);let u:URL;try{u=new URL(raw);}catch{throw new InventoryGatewayError('DELIVERY_ORDER_GATEWAY_NOT_CONFIGURED','Cổng giao nhận chưa được cấu hình',503,false);}if(!['http:','https:'].includes(u.protocol)||u.username||u.password||(process.env.NODE_ENV==='production'&&u.protocol!=='https:'))throw new InventoryGatewayError('DELIVERY_ORDER_GATEWAY_NOT_CONFIGURED','Cổng giao nhận chưa được cấu hình',503,false);u.pathname=u.pathname.replace(/\/$/,'');u.search='';u.hash='';return u.toString().replace(/\/$/,'');}function safeQuery(p:URLSearchParams){const n=new URLSearchParams();for(const[k,v]of p.entries())if(ALLOWED_QUERY_KEYS.has(k)&&v.length<=128)n.append(k,v);const s=n.toString();return s?`?${s}`:'';}function assertUuid(v:string,c:string,m:string){if(!UUID_PATTERN.test(v))throw new InventoryGatewayError(c,m,400,false);}function key(v:string|null|undefined){const k=String(v??'').trim();if(!/^[A-Za-z0-9._:-]{1,128}$/.test(k))throw new InventoryGatewayError('INVALID_IDEMPOTENCY_KEY','Khóa chống xử lý trùng không hợp lệ',400,false);return k;}async function req<T>({path,method,requestId,searchParams,body,idempotencyKey}:{path:string;method:'GET'|'POST';requestId:string;searchParams?:URLSearchParams;body?:unknown;idempotencyKey?:string}){const c=new AbortController();const t=setTimeout(()=>c.abort(),REQUEST_TIMEOUT_MS);try{const r=await fetch(`${baseUrl()}/api/delivery-orders${path}${searchParams?safeQuery(searchParams):''}`,{method,cache:'no-store',signal:c.signal,headers:{Authorization:`Bearer ${requireNppWorkforceSessionToken()}`,Accept:'application/json','x-request-id':requestId,...(body===undefined?{}:{'Content-Type':'application/json'}),...(idempotencyKey?{'Idempotency-Key':idempotencyKey}:{})},...(body===undefined?{}:{body:JSON.stringify(body)})});let p:CoreEnvelope<T>;try{p=await r.json() as CoreEnvelope<T>;}catch{throw new InventoryGatewayError('DELIVERY_ORDER_GATEWAY_RESPONSE_INVALID','Phản hồi giao nhận không hợp lệ',502,false);}if(!r.ok)throw new InventoryGatewayError(p.error?.code||'DELIVERY_ORDER_REQUEST_FAILED',p.error?.message||'Yêu cầu giao nhận không thành công',r.status,p.error?.retryable===true,p.error?.details??{});if(!Object.prototype.hasOwnProperty.call(p,'data'))throw new InventoryGatewayError('DELIVERY_ORDER_GATEWAY_RESPONSE_INVALID','Phản hồi giao nhận không hợp lệ',502,false);return p.data as T;}catch(e){if(e instanceof InventoryGatewayError)throw e;throw new InventoryGatewayError('DELIVERY_ORDER_GATEWAY_UNAVAILABLE','Cổng giao nhận tạm thời không khả dụng',503,true);}finally{clearTimeout(t);}}export function listDeliveryOrderEligibility<T>(requestId:string,searchParams:URLSearchParams):Promise<T>{return req<T>({path:'/eligibility',method:'GET',requestId,searchParams});}export function listDeliveryOrders<T>(requestId:string,searchParams:URLSearchParams):Promise<T>{return req<T>({path:'',method:'GET',requestId,searchParams});}export function getDeliveryOrder<T>(id:string,requestId:string):Promise<T>{assertUuid(id,'INVALID_DELIVERY_ORDER_ID','Mã chứng từ giao nhận không hợp lệ');return req<T>({path:`/${id}`,method:'GET',requestId});}export function createDeliveryOrder<T>(requestId:string,body:unknown,idempotencyKey:string|null):Promise<T>{return req<T>({path:'',method:'POST',requestId,body,idempotencyKey:key(idempotencyKey)});}export function transitionDeliveryOrder<T>(id:string,action:string,requestId:string,body:unknown,idempotencyKey:string|null):Promise<T>{assertUuid(id,'INVALID_DELIVERY_ORDER_ID','Mã chứng từ giao nhận không hợp lệ');if(!['confirm','cancel','pickup-handover','reverse-inventory-issue'].includes(action))throw new InventoryGatewayError('INVALID_DELIVERY_ORDER_ACTION','Thao tác giao nhận không hợp lệ',400,false);return req<T>({path:`/${id}/${action}`,method:'POST',requestId,body,idempotencyKey:key(idempotencyKey)});}export function listCustomerReturnEligibility<T>(requestId:string,searchParams:URLSearchParams):Promise<T>{return req<T>({path:'/customer-returns/eligibility',method:'GET',requestId,searchParams});}export function listCustomerReturns<T>(requestId:string,searchParams:URLSearchParams):Promise<T>{return req<T>({path:'/customer-returns',method:'GET',requestId,searchParams});}export function getCustomerReturn<T>(id:string,requestId:string):Promise<T>{assertUuid(id,'INVALID_CUSTOMER_RETURN_ID','Mã phiếu hàng khách trả không hợp lệ');return req<T>({path:`/customer-returns/${id}`,method:'GET',requestId});}export function createCustomerReturn<T>(requestId:string,body:unknown,idempotencyKey:string|null):Promise<T>{return req<T>({path:'/customer-returns',method:'POST',requestId,body,idempotencyKey:key(idempotencyKey)});}export function transitionCustomerReturn<T>(id:string,action:string,requestId:string,body:unknown,idempotencyKey:string|null):Promise<T>{assertUuid(id,'INVALID_CUSTOMER_RETURN_ID','Mã phiếu hàng khách trả không hợp lệ');if(!['receive','cancel'].includes(action))throw new InventoryGatewayError('INVALID_CUSTOMER_RETURN_ACTION','Thao tác hàng khách trả không hợp lệ',400,false);return req<T>({path:`/customer-returns/${id}/${action}`,method:'POST',requestId,body,idempotencyKey:key(idempotencyKey)});}export function freshDeliveryOrderKey(prefix:string){return `web-${prefix}-${randomUUID()}`;}
