import 'server-only';
import { randomUUID } from 'node:crypto';
import { createIdempotencyKey, isValidIdempotencyKey, normalizeIdempotencyKey } from '@npp/contracts';
import { requireNppWorkforceSessionToken } from './internal-auth-client';
const REQUEST_ID_PATTERN=/^[A-Za-z0-9._:-]{1,128}$/;const UUID_PATTERN=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const REQUEST_TIMEOUT_MS=8_000;const ALLOWED_QUERY_KEYS=new Set(['active','limit','offset','search','documentType','periodKey']);interface CoreEnvelope<T>{data?:T;error?:{code?:string;message?:string;retryable?:boolean;details?:unknown};requestId?:string}export class DocumentNumberingGatewayError extends Error{constructor(public readonly code:string,public readonly publicMessage:string,public readonly statusCode:number,public readonly retryable:boolean,public readonly details:unknown={}){super(publicMessage);this.name='DocumentNumberingGatewayError';}}export function resolveDocumentNumberingRequestId(value:string|null|undefined){const c=String(value??'').trim();return REQUEST_ID_PATTERN.test(c)?c:`web_${randomUUID()}`;}export function normalizeDocumentNumberingGatewayError(error:unknown){return error instanceof DocumentNumberingGatewayError?error:new DocumentNumberingGatewayError('DOCUMENT_NUMBERING_GATEWAY_UNAVAILABLE','Dữ liệu số chứng từ tạm thời chưa sẵn sàng',503,true);}function baseUrl(){const raw=process.env.CORE_API_INTERNAL_URL?.trim();if(!raw)throw new DocumentNumberingGatewayError('DOCUMENT_NUMBERING_GATEWAY_NOT_CONFIGURED','Cổng số chứng từ chưa được cấu hình',503,false);let u:URL;try{u=new URL(raw);}catch{throw new DocumentNumberingGatewayError('DOCUMENT_NUMBERING_GATEWAY_NOT_CONFIGURED','Cổng số chứng từ chưa được cấu hình',503,false);}if(!['http:','https:'].includes(u.protocol)||u.username||u.password||(process.env.NODE_ENV==='production'&&u.protocol!=='https:'))throw new DocumentNumberingGatewayError('DOCUMENT_NUMBERING_GATEWAY_NOT_CONFIGURED','Cổng số chứng từ chưa được cấu hình',503,false);u.pathname=u.pathname.replace(/\/$/,'');u.search='';u.hash='';return u.toString().replace(/\/$/,'');}function safeQuery(p:URLSearchParams){const n=new URLSearchParams();for(const[k,v]of p.entries())if(ALLOWED_QUERY_KEYS.has(k)&&v.length<=256)n.append(k,v);const s=n.toString();return s?`?${s}`:'';}function assertUuid(v:string,c:string,m:string){const n=String(v??'').trim();if(!UUID_PATTERN.test(n))throw new DocumentNumberingGatewayError(c,m,400,false);return n;}function mutationKey(value:string|undefined,operation:string){if(value===undefined||!value.trim())return createIdempotencyKey(operation);const normalized=normalizeIdempotencyKey(value);if(!normalized||!isValidIdempotencyKey(normalized))throw new DocumentNumberingGatewayError('INVALID_IDEMPOTENCY_KEY','Khóa chống xử lý trùng không hợp lệ',400,false);return normalized;}async function req<T>({method,path,requestId,searchParams,body,idempotencyKey}:{method:'GET'|'POST'|'PATCH';path:string;requestId:string;searchParams?:URLSearchParams;body?:unknown;idempotencyKey?:string}){const c=new AbortController();const t=setTimeout(()=>c.abort(),REQUEST_TIMEOUT_MS);try{const r=await fetch(`${baseUrl()}${path}${searchParams?safeQuery(searchParams):''}`,{method,cache:'no-store',signal:c.signal,headers:{Authorization:`Bearer ${requireNppWorkforceSessionToken()}`,Accept:'application/json','x-request-id':requestId,...(body===undefined?{}:{'Content-Type':'application/json'}),...(idempotencyKey?{'Idempotency-Key':idempotencyKey}:{})},...(body===undefined?{}:{body:JSON.stringify(body)})});let p:CoreEnvelope<T>;try{p=await r.json() as CoreEnvelope<T>;}catch{throw new DocumentNumberingGatewayError('DOCUMENT_NUMBERING_GATEWAY_RESPONSE_INVALID','Phản hồi số chứng từ không hợp lệ',502,false);}if(!r.ok)throw new DocumentNumberingGatewayError(p.error?.code||'DOCUMENT_NUMBERING_REQUEST_FAILED',p.error?.message||'Yêu cầu số chứng từ không thành công',r.status,p.error?.retryable===true,p.error?.details??{});if(!Object.prototype.hasOwnProperty.call(p,'data'))throw new DocumentNumberingGatewayError('DOCUMENT_NUMBERING_GATEWAY_RESPONSE_INVALID','Phản hồi số chứng từ không hợp lệ',502,false);return p.data as T;}catch(e){if(e instanceof DocumentNumberingGatewayError)throw e;throw new DocumentNumberingGatewayError('DOCUMENT_NUMBERING_GATEWAY_UNAVAILABLE','Cổng số chứng từ tạm thời không khả dụng',503,true);}finally{clearTimeout(t);}}export function listDocumentNumberSeries<T>(requestId:string,searchParams=new URLSearchParams()):Promise<T[]>{return req<T[]>({method:'GET',path:'/api/document-number-series',requestId,searchParams});}export function createDocumentNumberSeries<T>(requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return req<T>({method:'POST',path:'/api/document-number-series',requestId,body,idempotencyKey:mutationKey(idempotencyKey,'document-number-series-create')});}export function getDocumentNumberSeries<T>(id:string,requestId:string):Promise<T>{return req<T>({method:'GET',path:`/api/document-number-series/${assertUuid(id,'INVALID_SERIES_ID','Mã series không hợp lệ')}`,requestId});}export function patchDocumentNumberSeries<T>(id:string,requestId:string,body:unknown):Promise<T>{return req<T>({method:'PATCH',path:`/api/document-number-series/${assertUuid(id,'INVALID_SERIES_ID','Mã series không hợp lệ')}`,requestId,body});}export function listDocumentNumberAllocations<T>(id:string,requestId:string,searchParams=new URLSearchParams()):Promise<T>{return req<T>({method:'GET',path:`/api/document-number-series/${assertUuid(id,'INVALID_SERIES_ID','Mã series không hợp lệ')}/allocations`,requestId,searchParams});}export function allocateDocumentNumber<T>(id:string,requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return req<T>({method:'POST',path:`/api/document-number-series/${assertUuid(id,'INVALID_SERIES_ID','Mã series không hợp lệ')}/allocate`,requestId,body,idempotencyKey:mutationKey(idempotencyKey,'document-number-allocate')});}
