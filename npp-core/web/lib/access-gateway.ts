import 'server-only';
import { randomUUID } from 'node:crypto';
import { requireNppWorkforceSessionToken } from './internal-auth-client';
const REQUEST_ID_PATTERN=/^[A-Za-z0-9._:-]{1,128}$/;const UUID_PATTERN=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const REQUEST_TIMEOUT_MS=8_000;const ROLE_PAGE_SIZE=1000;const ALLOWED_QUERY_KEYS=new Set(['active','limit','offset','q','search']);interface CoreEnvelope<T>{data?:T;error?:{code?:string;message?:string;retryable?:boolean;details?:unknown};requestId?:string}
export class AccessGatewayError extends Error{constructor(public readonly code:string,public readonly publicMessage:string,public readonly statusCode:number,public readonly retryable:boolean,public readonly details:unknown={}){super(publicMessage);this.name='AccessGatewayError';}}
export function resolveAccessRequestId(value:string|null|undefined){const c=String(value??'').trim();return REQUEST_ID_PATTERN.test(c)?c:`web_${randomUUID()}`;}export function normalizeAccessGatewayError(error:unknown){return error instanceof AccessGatewayError?error:new AccessGatewayError('ACCESS_GATEWAY_UNAVAILABLE','Dữ liệu phân quyền tạm thời chưa sẵn sàng',503,true);}function requiredServerValue(name:'CORE_API_INTERNAL_URL'|'CORE_API_SERVER_TOKEN'){if(name==='CORE_API_SERVER_TOKEN')return requireNppWorkforceSessionToken();const v=process.env[name]?.trim();if(!v)throw new AccessGatewayError('ACCESS_GATEWAY_NOT_CONFIGURED','Cổng phân quyền chưa được cấu hình',503,false);return v;}function coreApiBaseUrl(){let u:URL;try{u=new URL(requiredServerValue('CORE_API_INTERNAL_URL'));}catch{throw new AccessGatewayError('ACCESS_GATEWAY_NOT_CONFIGURED','Cổng phân quyền chưa được cấu hình',503,false);}if(!['http:','https:'].includes(u.protocol)||u.username||u.password||(process.env.NODE_ENV==='production'&&u.protocol!=='https:'))throw new AccessGatewayError('ACCESS_GATEWAY_NOT_CONFIGURED','Cổng phân quyền chưa được cấu hình',503,false);u.pathname=u.pathname.replace(/\/$/,'');u.search='';u.hash='';return u.toString().replace(/\/$/,'');}function safeQuery(p:URLSearchParams){const n=new URLSearchParams();for(const[k,v]of p.entries())if(ALLOWED_QUERY_KEYS.has(k)&&v.length<=128)n.append(k,v);const s=n.toString();return s?`?${s}`:'';}
async function requestAccess<T>({path,method,requestId,searchParams,body,idempotencyKey}:{path:string;method:'GET'|'POST'|'PATCH';requestId:string;searchParams?:URLSearchParams;body?:unknown;idempotencyKey?:string}){const c=new AbortController();const t=setTimeout(()=>c.abort(),REQUEST_TIMEOUT_MS);try{const r=await fetch(`${coreApiBaseUrl()}/api/access${path}${searchParams?safeQuery(searchParams):''}`,{method,cache:'no-store',signal:c.signal,headers:{Authorization:`Bearer ${requiredServerValue('CORE_API_SERVER_TOKEN')}`,Accept:'application/json','x-request-id':requestId,...(body===undefined?{}:{'Content-Type':'application/json'}),...(idempotencyKey?{'Idempotency-Key':idempotencyKey}:{})},...(body===undefined?{}:{body:JSON.stringify(body)})});let p:CoreEnvelope<T>;try{p=await r.json() as CoreEnvelope<T>;}catch{throw new AccessGatewayError('ACCESS_GATEWAY_RESPONSE_INVALID','Phản hồi phân quyền không hợp lệ',502,false);}if(!r.ok)throw new AccessGatewayError(p.error?.code||'ACCESS_REQUEST_FAILED',p.error?.message||'Yêu cầu phân quyền không thành công',r.status,p.error?.retryable===true,p.error?.details??{});if(!Object.prototype.hasOwnProperty.call(p,'data'))throw new AccessGatewayError('ACCESS_GATEWAY_RESPONSE_INVALID','Phản hồi phân quyền không hợp lệ',502,false);return p.data as T;}catch(e){if(e instanceof AccessGatewayError)throw e;throw new AccessGatewayError('ACCESS_GATEWAY_UNAVAILABLE','Cổng phân quyền tạm thời không khả dụng',503,true);}finally{clearTimeout(t);}}
export function listAccessPermissions<T>(requestId:string):Promise<T>{return requestAccess<T>({path:'/permissions',method:'GET',requestId});}export function listAccessRoles<T>(requestId:string,searchParams:URLSearchParams):Promise<T>{return requestAccess<T>({path:'/roles',method:'GET',requestId,searchParams});}export async function listAllAccessRoles<T>(requestId:string,searchParams=new URLSearchParams()):Promise<T[]>{const f=new URLSearchParams(searchParams);f.delete('limit');f.delete('offset');const roles:T[]=[];let offset=0;while(true){const p=new URLSearchParams(f);p.set('limit',String(ROLE_PAGE_SIZE));p.set('offset',String(offset));const page=await requestAccess<T[]>({path:'/roles',method:'GET',requestId,searchParams:p});roles.push(...page);if(page.length<ROLE_PAGE_SIZE)return roles;offset+=page.length;}}function assertId(id:string,kind:'ROLE'|'USER'){if(!UUID_PATTERN.test(id))throw new AccessGatewayError(`INVALID_${kind}_ID`,kind==='ROLE'?'Mã vai trò không hợp lệ':'Mã người dùng không hợp lệ',400,false);return id;}export function getAccessRole<T>(id:string,requestId:string):Promise<T>{return requestAccess<T>({path:`/roles/${assertId(id,'ROLE')}`,method:'GET',requestId});}export function createAccessRole<T>(requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestAccess<T>({path:'/roles',method:'POST',requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}export function patchAccessRole<T>(id:string,requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestAccess<T>({path:`/roles/${assertId(id,'ROLE')}`,method:'PATCH',requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}export function listAccessUsers<T>(requestId:string,searchParams:URLSearchParams):Promise<T>{return requestAccess<T>({path:'/users',method:'GET',requestId,searchParams});}export function getAccessUser<T>(id:string,requestId:string):Promise<T>{return requestAccess<T>({path:`/users/${assertId(id,'USER')}`,method:'GET',requestId});}export function createAccessUser<T>(requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestAccess<T>({path:'/users',method:'POST',requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}export function patchAccessUser<T>(id:string,requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestAccess<T>({path:`/users/${assertId(id,'USER')}`,method:'PATCH',requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}export function patchAccessUserRoles<T>(id:string,requestId:string,body:unknown,idempotencyKey?:string):Promise<T>{return requestAccess<T>({path:`/users/${assertId(id,'USER')}/roles`,method:'PATCH',requestId,body,idempotencyKey:idempotencyKey?.trim()||`web-${randomUUID()}`});}
