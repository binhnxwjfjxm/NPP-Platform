import 'server-only';
import { randomUUID } from 'node:crypto';
import { requireNppWorkforceSessionToken } from './internal-auth-client';
type CoreEnvelope<T>={data?:T|null;error?:{code?:string;message?:string;retryable?:boolean;details?:unknown}};type PageState={hasMore:boolean;nextCursor:string|null};export type AuditHistoryRow={auditId:string;actorId:string;employeeId:string|null;sourceApp:string;requestId:string;action:string;resourceType:string;resourceId:string|null;occurredAt:string;hasBeforeData:boolean;hasAfterData:boolean;hasMetadata:boolean};export type ImportExportHistoryRow={jobId:string;direction:'IMPORT'|'EXPORT';definitionKey:string;definitionVersion:string;format:string;status:string;actorId:string;employeeId:string|null;sourceApp:string;requestId:string;normalizedFilters:Record<string,unknown>;effectiveScopes:Record<string,unknown>;businessTimezone:string;sourceAsOf:string|null;rowCount:string|null;hasResult:boolean;resultChecksumSha256:string|null;failureCode:string|null;requestedAt:string;startedAt:string|null;completedAt:string|null};export type AuditHistoryData={generatedAt:string;timezone:string;rows:AuditHistoryRow[];page:PageState};export type ImportExportHistoryData={generatedAt:string;timezone:string;rows:ImportExportHistoryRow[];page:PageState};export class OperationsHistoryGatewayError extends Error{constructor(public readonly code:string,public readonly publicMessage:string,public readonly statusCode:number,public readonly retryable:boolean){super(publicMessage);this.name='OperationsHistoryGatewayError';}}function baseUrl(){const raw=process.env.CORE_API_INTERNAL_URL?.trim();if(!raw)throw new OperationsHistoryGatewayError('REPORTING_GATEWAY_NOT_CONFIGURED','Lịch sử vận hành chưa được cấu hình',503,false);let u:URL;try{u=new URL(raw);}catch{throw new OperationsHistoryGatewayError('REPORTING_GATEWAY_NOT_CONFIGURED','Lịch sử vận hành chưa được cấu hình',503,false);}if(!['http:','https:'].includes(u.protocol)||u.username||u.password||(process.env.NODE_ENV==='production'&&u.protocol!=='https:'))throw new OperationsHistoryGatewayError('REPORTING_GATEWAY_NOT_CONFIGURED','Lịch sử vận hành chưa được cấu hình',503,false);return u.toString().replace(/\/$/,'');}async function getHistory<T>(path:string,params:Record<string,string|undefined>):Promise<T>{const q=new URLSearchParams();for(const[k,v]of Object.entries(params))if(v)q.set(k,v);const c=new AbortController();const t=setTimeout(()=>c.abort(),10_000);try{const r=await fetch(`${baseUrl()}${path}${q.size?`?${q}`:''}`,{method:'GET',cache:'no-store',signal:c.signal,headers:{Authorization:`Bearer ${requireNppWorkforceSessionToken()}`,Accept:'application/json','x-request-id':`web_${randomUUID()}`}});const p=await r.json().catch(()=>null) as CoreEnvelope<T>|null;if(!p)throw new OperationsHistoryGatewayError('REPORTING_GATEWAY_RESPONSE_INVALID','Phản hồi lịch sử vận hành không hợp lệ',502,false);if(!r.ok)throw new OperationsHistoryGatewayError(p.error?.code||'REPORTING_REQUEST_FAILED',p.error?.message||'Không tải được lịch sử vận hành',r.status,p.error?.retryable===true);if(!Object.prototype.hasOwnProperty.call(p,'data')||p.data===null)throw new OperationsHistoryGatewayError('REPORTING_GATEWAY_RESPONSE_INVALID','Phản hồi lịch sử vận hành không hợp lệ',502,false);return p.data as T;}catch(e){if(e instanceof OperationsHistoryGatewayError)throw e;throw new OperationsHistoryGatewayError('REPORTING_GATEWAY_UNAVAILABLE','Lịch sử vận hành tạm thời chưa khả dụng',503,true);}finally{clearTimeout(t);}}export function getAuditHistory(params:Record<string,string|undefined>):Promise<AuditHistoryData>{return getHistory('/api/reporting/audit-history',params);}export function getImportExportHistory(params:Record<string,string|undefined>):Promise<ImportExportHistoryData>{return getHistory('/api/reporting/import-export-history',params);}
