import 'server-only';
import { createHash, randomUUID } from 'node:crypto';
import { InventoryGatewayError } from './inventory-gateway';
import { requireNppWorkforceSessionToken } from './internal-auth-client';
const REQUEST_TIMEOUT_MS=30_000;const UUID_PATTERN=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;const SAFE_RESOURCE_PATTERN=/^(warehouses|routes|vehicles|drivers|eligible-delivery-orders|trips)$/;const ALLOWED_QUERY_KEYS=new Set(['active','warehouseId','status','limit','offset']);const READ_ACTIONS=new Set(['dispatch','reconciliation']);const WRITE_ACTIONS=new Set(['assign','unassign','reorder','plan','reopen','lock','dispatch','return-receipts','close']);interface CoreEnvelope<T>{data?:T;error?:{code?:string;message?:string;retryable?:boolean;details?:unknown}}
function baseUrl(){const raw=process.env.CORE_API_INTERNAL_URL?.trim();if(!raw)throw new InventoryGatewayError('LOGISTICS_GATEWAY_NOT_CONFIGURED','Cổng điều phối chưa được cấu hình',503,false);let u:URL;try{u=new URL(raw);}catch{throw new InventoryGatewayError('LOGISTICS_GATEWAY_NOT_CONFIGURED','Cổng điều phối chưa được cấu hình',503,false);}if(!['http:','https:'].includes(u.protocol)||u.username||u.password||(process.env.NODE_ENV==='production'&&u.protocol!=='https:'))throw new InventoryGatewayError('LOGISTICS_GATEWAY_NOT_CONFIGURED','Cổng điều phối chưa được cấu hình',503,false);u.pathname=u.pathname.replace(/\/$/,'');u.search='';u.hash='';return u.toString().replace(/\/$/,'');}function safeQuery(p:URLSearchParams){const n=new URLSearchParams();for(const[k,v]of p.entries()){if(!ALLOWED_QUERY_KEYS.has(k)||v.length>128)continue;if(k==='status'&&v==='all')continue;n.append(k,v);}const s=n.toString();return s?`?${s}`:'';}function assertResource(r:string){if(!SAFE_RESOURCE_PATTERN.test(r))throw new InventoryGatewayError('INVALID_LOGISTICS_RESOURCE','Tài nguyên điều phối không hợp lệ',400,false);}function assertUuid(v:string,c:string,m:string){if(!UUID_PATTERN.test(v))throw new InventoryGatewayError(c,m,400,false);}function key(v:string|null|undefined){const k=String(v??'').trim();if(!/^[A-Za-z0-9._:-]{1,128}$/.test(k))throw new InventoryGatewayError('INVALID_IDEMPOTENCY_KEY','Khóa chống xử lý trùng không hợp lệ',400,false);return k;}const CORE_SAFE_IDEMPOTENCY_KEY_PATTERN=/^[A-Za-z0-9._-]{1,128}$/;function coreMutationKey(v:string|null|undefined){const candidate=key(v);return CORE_SAFE_IDEMPOTENCY_KEY_PATTERN.test(candidate)?candidate:createHash('sha256').update(candidate,'utf8').digest('hex');}async function req<T>({path,method,requestId,searchParams,body,idempotencyKey}:{path:string;method:'GET'|'POST'|'PUT';requestId:string;searchParams?:URLSearchParams;body?:unknown;idempotencyKey?:string}){const c=new AbortController();const t=setTimeout(()=>c.abort(),REQUEST_TIMEOUT_MS);try{const r=await fetch(`${baseUrl()}/api/logistics${path}${searchParams?safeQuery(searchParams):''}`,{method,cache:'no-store',signal:c.signal,headers:{Authorization:`Bearer ${requireNppWorkforceSessionToken()}`,Accept:'application/json','x-request-id':requestId,...(body===undefined?{}:{'Content-Type':'application/json'}),...(idempotencyKey?{'Idempotency-Key':idempotencyKey}:{})},...(body===undefined?{}:{body:JSON.stringify(body)})});let p:CoreEnvelope<T>;try{p=await r.json() as CoreEnvelope<T>;}catch{throw new InventoryGatewayError('LOGISTICS_GATEWAY_RESPONSE_INVALID','Phản hồi điều phối không hợp lệ',502,false);}if(!r.ok)throw new InventoryGatewayError(p.error?.code||'LOGISTICS_REQUEST_FAILED',p.error?.message||'Yêu cầu điều phối không thành công',r.status,p.error?.retryable===true,p.error?.details??{});if(!Object.prototype.hasOwnProperty.call(p,'data'))throw new InventoryGatewayError('LOGISTICS_GATEWAY_RESPONSE_INVALID','Phản hồi điều phối không hợp lệ',502,false);return p.data as T;}catch(e){if(e instanceof InventoryGatewayError)throw e;throw new InventoryGatewayError('LOGISTICS_GATEWAY_UNAVAILABLE','Cổng điều phối tạm thời không khả dụng',503,true);}finally{clearTimeout(t);}}export function listLogisticsResource<T>(resource:string,requestId:string,searchParams:URLSearchParams):Promise<T>{assertResource(resource);return req<T>({path:`/${resource}`,method:'GET',requestId,searchParams});}export function createLogisticsResource<T>(resource:string,requestId:string,body:unknown,idempotencyKey:string|null):Promise<T>{assertResource(resource);if(resource==='eligible-delivery-orders'||resource==='warehouses')throw new InventoryGatewayError('INVALID_LOGISTICS_RESOURCE','Tài nguyên điều phối chỉ cho phép đọc',400,false);return req<T>({path:`/${resource}`,method:'POST',requestId,body,idempotencyKey:coreMutationKey(idempotencyKey)});}export function getDeliveryTrip<T>(tripId:string,requestId:string):Promise<T>{assertUuid(tripId,'INVALID_TRIP_ID','Mã chuyến giao không hợp lệ');return req<T>({path:`/trips/${tripId}`,method:'GET',requestId});}export function getDeliveryTripAction<T>(tripId:string,action:string,requestId:string):Promise<T>{assertUuid(tripId,'INVALID_TRIP_ID','Mã chuyến giao không hợp lệ');if(!READ_ACTIONS.has(action))throw new InventoryGatewayError('INVALID_TRIP_ACTION','Thao tác chuyến giao không hợp lệ',400,false);return req<T>({path:`/trips/${tripId}/${action}`,method:'GET',requestId});}export function updateDeliveryTrip<T>(tripId:string,requestId:string,body:unknown,idempotencyKey:string|null):Promise<T>{assertUuid(tripId,'INVALID_TRIP_ID','Mã chuyến giao không hợp lệ');return req<T>({path:`/trips/${tripId}`,method:'PUT',requestId,body,idempotencyKey:coreMutationKey(idempotencyKey)});}export function transitionDeliveryTrip<T>(tripId:string,action:string,requestId:string,body:unknown,idempotencyKey:string|null):Promise<T>{assertUuid(tripId,'INVALID_TRIP_ID','Mã chuyến giao không hợp lệ');if(!WRITE_ACTIONS.has(action))throw new InventoryGatewayError('INVALID_TRIP_ACTION','Thao tác chuyến giao không hợp lệ',400,false);return req<T>({path:`/trips/${tripId}/${action}`,method:'POST',requestId,body,idempotencyKey:coreMutationKey(idempotencyKey)});}export function freshLogisticsKey(prefix:string){return `web-logistics-${prefix}-${randomUUID()}`;}